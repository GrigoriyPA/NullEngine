#pragma once

#include <SFML/Graphics/Color.hpp>
#include <cstdint>

namespace null_engine {

static constexpr uint32_t kDefaultFontSize = 20;

static const sf::Color kDefaultTextColor = sf::Color(255, 255, 255);

}  // namespace null_engine
