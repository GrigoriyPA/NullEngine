#pragma once

#include <null_engine/util/geometry/vector_3d.hpp>

namespace null_engine::tests {

static const util::Vec3 kWhite = util::Vec3(1.0);
static const util::Vec3 kRed = util::Vec3(1.0, 0.0, 0.0);

}  // namespace null_engine::tests
